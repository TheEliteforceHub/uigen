Run a security audit of the project dependencies:

1. Run `npm audit` to find vulnerable installed packages
2. Run `npm audit fix` to apply updates
3. Run `npm run test` to verify the updates didn't break anything
